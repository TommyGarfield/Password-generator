#Password generator

#Get password length from user, store in variable password_length
password_length = int(input("Enter the desired password length "))

#checking that variable is stored
print(password_length)
